{
  "name": "bus-back",
  "version": "1.0.0",
  "description": "Bus Back — Endless runner car chase game",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "dev": "npx serve -l 3333 .",
    "build": "rm -rf www && mkdir -p www && cp -r index.html src www/ && (cp -r assets www/ 2>/dev/null || true)",
    "build:ios": "npx cap add ios && npx cap sync ios && npx cap open ios",
    "build:android": "npx cap add android && npx cap sync android && npx cap open android",
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
